package com.advertisex.dataengineering.storage;

public class QueryEngine {

    public void executeQuery(String query) {
        // Execute query using underlying storage engine (e.g., SQL, NoSQL)
        System.out.println("Query executed: " + query);
    }
}
